<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;net45</TargetFrameworks>
    <PackageId>GoCommando</PackageId>    
    <Company />
    <PackageProjectUrl>https://github.com/rebus-org/GoCommando</PackageProjectUrl>    
  </PropertyGroup>  
  <PropertyGroup>
    <DefineConstants Condition=" '$(TargetFrameworkVersion)' == 'v4.5' ">NETFX</DefineConstants>
  </PropertyGroup>
</Project>